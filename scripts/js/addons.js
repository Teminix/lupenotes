const Addons = {
  initiate:function(addons) {
    if (addons.includes("colortags")) {
      let color = document.getElementsByTagName("c");
      /*
      Mutation code:

      var observer = new mutationObs(function(mutations){
    mutations.forEach(function(mutation){
        if(mutation.type == "attributes") {console.log(mutation)}})})
        observer.observe(element, {
    attributes:true})
      */

    }
    if (addons.includes("circles")) {

  }
}}
